settings = {}
def init_settings(nid, nauth_host_location, nkey, sitename):
    settings['nid'] = nid
    settings['nauth_host_location'] = nauth_host_location
    settings['nkey'] = nkey
    settings['sitename'] = sitename